/**
 * 资讯页列表卡片底部「查看详情 →」样式，与在招职位 JobItemCard 一致。
 * 用于本身可点击的 `button` / 链接触发区域。
 */
export const DETAIL_VIEW_CTA_CLASS =
  "text-sky-500 text-sm font-medium hover:text-sky-600";

/**
 * 整行/整卡可点且父级已加 Tailwind `group` 时，子级「查看详情 →」用此类以随父级悬停变色。
 */
export const DETAIL_VIEW_CTA_GROUP_CLASS =
  "text-sky-500 text-sm font-medium group-hover:text-sky-600";
